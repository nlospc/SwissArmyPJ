{
  "name": "pmbrain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first PM knowledge brain with SQLite, Obsidian projection, MCP, and CLI workflows.",
  "exports": {
    ".": "./src/sdk/index.ts",
    "./cli": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "start": "bun run src/cli.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/bun": "latest"
  }
}
